<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>¿Querés ser mi San Valentín?</title>
<style>
    body {
        margin: 0;
        padding: 0;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: black;
        font-family: 'Arial', sans-serif;
        color: white;
        overflow: hidden;
    }

    .card {
        text-align: center;
        padding: 40px;
        border: 4px solid #ff00ff;
        border-radius: 20px;
        box-shadow: 0 0 25px #ff00ff, 0 0 50px #ff1493;
        background: rgba(20, 0, 20, 0.9);
        position: relative;
    }

    h1 {
        font-size: 2.5em;
        color: #ff69b4;
        text-shadow: 0 0 10px #ff00ff;
    }

    .buttons {
        margin-top: 30px;
    }

    button {
        padding: 15px 30px;
        margin: 10px;
        font-size: 1.2em;
        border-radius: 15px;
        border: none;
        cursor: pointer;
        transition: 0.3s ease;
    }

    #yes {
        background: #ff1493;
        color: white;
        box-shadow: 0 0 15px #ff00ff;
    }

    #yes:hover {
        transform: scale(1.1);
        box-shadow: 0 0 25px #ff69b4;
    }

    #no {
        background: #444;
        color: white;
    }

    .heart {
        position: absolute;
        color: #ff1493;
        font-size: 20px;
        animation: float 6s linear infinite;
    }

    @keyframes float {
        0% {
            transform: translateY(100vh);
            opacity: 0;
        }
        10% {
            opacity: 1;
        }
        100% {
            transform: translateY(-10vh);
            opacity: 0;
        }
    }

    .celebration {
        animation: glow 1s infinite alternate;
    }

    @keyframes glow {
        from { box-shadow: 0 0 20px #ff00ff; }
        to { box-shadow: 0 0 60px #ff69b4; }
    }

    .error-screen {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: black;
        display: none;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        z-index: 999;
        text-align: center;
    }

    .error-box {
        border: 4px solid #ff1493;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 0 30px #ff00ff, 0 0 60px #ff1493;
        background: rgba(30, 0, 30, 0.95);
    }

    .error-box h2 {
        color: #ff4da6;
        margin-bottom: 20px;
        text-shadow: 0 0 10px #ff00ff;
    }

    .error-box button {
        background: #ff1493;
        color: white;
        padding: 12px 25px;
        border-radius: 12px;
        border: none;
        cursor: pointer;
        font-size: 1em;
        box-shadow: 0 0 15px #ff00ff;
    }
</style>
</head>
<body>

<div class="card" id="card">
    <h1>¿Querés ser mi San Valentín? 💖</h1>
    <div class="buttons">
        <button id="yes">Sí 💕</button>
        <button id="no">No</button>
    </div>
    <p id="message"></p>
</div>

<div class="error-screen" id="errorScreen">
    <div class="error-box">
        <h2>⚠ ERROR CRÍTICO ⚠</h2>
        <p>Solo existe una opción válida... y es decir que Sí 💖</p>
        <button onclick="closeError()">Entendido 💕</button>
    </div>
</div>

<script>
    const yesBtn = document.getElementById("yes");
    const noBtn = document.getElementById("no");
    const message = document.getElementById("message");
    const card = document.getElementById("card");

    yesBtn.addEventListener("click", () => {
        card.classList.add("celebration");
        document.body.style.background = "radial-gradient(circle, #330033, black)";
        message.innerHTML = "💖 ¡GRACIAS POR DECIR QUE SI 💖<br>Me haces la persona más feliz del mundo siempre lindurita.";
        createHearts(40);
    });

    noBtn.addEventListener("click", () => {
        document.getElementById("errorScreen").style.display = "flex";
    });

    function closeError() {
        document.getElementById("errorScreen").style.display = "none";
    }

    function createHearts(num) {
        for (let i = 0; i < num; i++) {
            let heart = document.createElement("div");
            heart.classList.add("heart");
            heart.innerHTML = "❤";
            heart.style.left = Math.random() * 100 + "vw";
            heart.style.fontSize = (Math.random() * 20 + 15) + "px";
            heart.style.animationDuration = (Math.random() * 3 + 3) + "s";
            document.body.appendChild(heart);

            setTimeout(() => {
                heart.remove();
            }, 6000);
        }
    }
</script>

</body>
</html>
